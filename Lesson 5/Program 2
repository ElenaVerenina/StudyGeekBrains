using System;
using System.IO;

namespace Lesson_5___2
{
    class Program
    {
        static void Main(string[] args)
        {
            string filename = "startup.txt";
            File.AppendAllText(filename, Environment.NewLine);
            File.AppendAllText(filename, DateTime.Now.ToString("HH:mm:ss"));

        }
    }
}
